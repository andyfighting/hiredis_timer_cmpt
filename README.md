# hiredis_timer_cmpt
> An redis timer task component based on Hiredis source code development.

# Compile:
    ```make
       cd hiredis_timer_cmpt 
       make redis_timer
    ```
 
    ```use
      ./redis_timer -h 
    ```


